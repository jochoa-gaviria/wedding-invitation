<svg width="100%" height="100%" viewBox="0 0 800 600" xmlns="http://www.w3.org/2000/svg">
  <rect width="100%" height="100%" fill="#f0fdf4" />
  <rect width="100%" height="100%" fill="none" stroke="#16a34a" stroke-width="4" stroke-dasharray="8 4" />
  <text x="50%" y="50%" font-family="Arial, sans-serif" font-size="24" text-anchor="middle" fill="#14532d">
    <tspan id="dimensions">800×600</tspan>
  </text>
  <style>
    @media (prefers-color-scheme: dark) {
      rect:first-child { fill: #052e16; }
      text { fill: #86efac; }
    }
  </style>
  <script type="text/javascript">
    const svg = document.querySelector('svg');
    const dimensions = document.getElementById('dimensions');
    const urlParams = new URLSearchParams(window.location.search);
    const width = urlParams.get('width') || 800;
    const height = urlParams.get('height') || 600;
    
    svg.setAttribute('viewBox', `0 0 ${width} ${height}`);
    dimensions.textContent = `${width}×${height}`;
  </script>
</svg>